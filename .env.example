# Ceche Web — Environment Variables
# Copy to .env.local or set in your deploy platform (Vercel: PUBLIC_API_URL)

# Base URL of the Ceche HTTP API (see github.com/nekwasar/ceche)
# Leave empty to use same-origin relative paths (e.g. behind Nginx)
PUBLIC_API_URL=https://api.ceche.app
